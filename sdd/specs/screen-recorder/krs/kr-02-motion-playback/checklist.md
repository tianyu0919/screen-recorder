# Verification Checklist: kr-02-motion-playback

> 每条客观可验证，全部勾选后方可关闭本 KR。
> 验证记录：Task 4.1 已于 2026-08-20 使用 kr-01 真实会话逐项完成集成自测；Retina 坐标于 2026-08-25 的窗口固定画布复验中再次确认。

## Functional Verification
- [x] kr-01 真实录制会话可完整加载，视频时长与事件时间轴一致
- [x] 每次点击事件前 ~200ms 自动生成缩放关键帧，点击区域成为运镜焦点
- [x] 无操作超过回归阈值后相机回归 1.0x 全景
- [x] 密集点击（间隔 < 停留时长）时相机直接过渡，不插入多余回归关键帧
- [x] 运镜规则参数（目标倍率/停留时长/回归阈值）修改后预览即时生效
- [x] 逐帧采样 zoom 曲线连续、无瞬时跳变（无生硬线性切换）
- [x] 画面边缘点击时相机被钳制在画布内，不出现黑边穿帮
- [x] 点击时刻叠加波纹动画
- [x] 播放器支持播放/暂停/拖拽 seek，seek 后画面立即呈现对应时间点合成结果

## Edge Case Verification
- [x] clicks 为空的会话（降级录制）全程 1.0x 全景正常播放，并有"无运镜数据"提示
- [x] events.json 损坏/版本不兼容时提示明确，不进入预览、无原始堆栈
- [x] 5K 源视频超纹理上限时降采样正常、UI 明示输出分辨率变化、不花屏
- [x] scaleFactor=2 显示器录制的会话，运镜落点与画面实际点击位置目视重合

## Code Quality & Performance
- [x] 无新增 TypeScript 类型错误与 lint 告警；生产路径无调试残留
- [x] 渲染合成器与相机求值器为纯模块（不依赖 DOM/实时时钟），可被 kr-03 Worker 导出直接复用
- [x] 预览播放主流机型无明显掉帧
